/**
 * problems.js — backward-compat shim
 *
 * All problem data now lives in data.js (window.LC_DATA).
 * This file is kept for any legacy script that imports it.
 *
 * DO NOT add problems here — edit data.js instead.
 * After data.js loads, window.TP_PROBLEMS is already set globally.
 */
// No-op: data.js already sets window.TP_PROBLEMS and window.SW_PROBLEMS
